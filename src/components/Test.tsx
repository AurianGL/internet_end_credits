import { useContext } from "react";

const Test: React.FC = ({ children }) => {
	return <div>{children}</div>;
};

export default Test;

export const all_nigt = () => {
	return <div> </div>;
};

export const trying: React.FC = () => {
	return (
		<div>
			I like it now <br /> match stuff
		</div>
	);
};
